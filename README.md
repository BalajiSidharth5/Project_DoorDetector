# Project_DoorDetector
Door state detector built on the Adafruit HUZZAH32, using IR reflection to sense open or closed position without contact switches. Debounces button input in firmware. Runs on rechargeable AA batteries connected directly to the microcontroller. Moving from breadboard prototype to a custom PCB for a compact 3D printed enclosure.
